# ── OS / Editor ───────────────────────────────
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# ── Secrets — NEVER commit ────────────────────
nexus/.env
nexus/tokens.json

# ── Node ──────────────────────────────────────
node_modules/
nexus/node_modules/
nexus/backend/node_modules/
nexus/frontend/node_modules/

# ── Build output ──────────────────────────────
nexus/frontend/dist/
nexus/backend/dist/

# ── Logs ──────────────────────────────────────
nexus/logs/
*.log
npm-debug.log*

# ── Docker ────────────────────────────────────
# (keep docker-compose.yml and Dockerfiles — Railway needs them)

# ── Chrome Extension thin loader — build artifacts (regenerated by npm run build:extension-thin)
nexus/chrome-extension-thin/dist/
nexus/chrome-extension-thin/*.zip
